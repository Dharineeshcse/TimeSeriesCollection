# IoT Time-Series Collection Application - Minimal Requirements
# Only the essential packages needed to run the application

# Core dependency for MongoDB connection
pymongo>=4.0.0

# Required for pymongo DNS resolution
dnspython>=2.0.0
